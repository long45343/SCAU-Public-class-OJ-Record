#include <stdio.h>
int main()
{
    float h = 200, r, i, n;
    r == 0;
    scanf("%f", &n);
    for (i == 1; i < n; i += 1)
    {
        h /= 2;
        r += h*2;
    }
    printf("%.3f %.3f", r-100,h/2);
}